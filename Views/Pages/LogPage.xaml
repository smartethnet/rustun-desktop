<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Rustun.Views.Pages.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rustun.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Unloaded="Page_Unloaded"
    xmlns:models="using:Rustun.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  Spacing between rows  -->
        <x:Double x:Key="VerticalSpacing">4</x:Double>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PageHeader" MaxWidth="1064" Margin="36,24,36,0" Style="{StaticResource TitleTextBlockStyle}" Text="Logs" />

        <Grid Grid.Row="1" Padding="36,0,36,36">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 工具栏 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBox PlaceholderText="Filter" Width="300" Height="0"/>
                <CommandBar DefaultLabelPosition="Right" VerticalAlignment="Bottom">
                    <AppBarButton x:Name="ClearButton" Icon="Clear" Label="Clear" Click="ClearButton_Click"/>
                    <AppBarButton Icon="Save" Label="Export"/>
                </CommandBar>
            </StackPanel>

            <!-- 日志显示区域 -->
            <Border Grid.Row="1" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefault}" Margin="0 8 0 8" Padding="16">
                <ScrollViewer x:Name="LogScrollViewer">
                    <ItemsRepeater x:Name="LogItemsControl" ItemsSource="{x:Bind _logService.LogEntries}">
                        <ItemsRepeater.Layout>
                            <StackLayout Orientation="Vertical"/>
                        </ItemsRepeater.Layout>

                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="models:LogEntry">
                                <TextBlock Text="{x:Bind Message}" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
